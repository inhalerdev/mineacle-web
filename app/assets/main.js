const toast=document.getElementById('toast'),banList=document.getElementById('banList'),banSearch=document.getElementById('banSearch'),banCount=document.getElementById('banCount'),banModal=document.getElementById('banModal');function showToast(t,s){if(!toast)return;toast.querySelector('strong').textContent=t;toast.querySelector('span').textContent=s;toast.classList.remove('show');void toast.offsetWidth;toast.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>toast.classList.remove('show'),3000)}function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function actionButton(ban){if(ban.ipban)return `<button class="btn soft disabled" disabled><img src="assets/lock.svg" alt=""> No Action</button>`; if(ban.can_pay)return `<a class="btn red" href="${escapeHtml(ban.unban_url)}">${escapeHtml(ban.price)} Unban</a>`; return `<button class="btn soft" type="button" data-info="${escapeHtml(ban.id)}">View</button>`}function renderBans(rows){if(!banList)return;if(!rows.length){banList.innerHTML='<div class="empty">No banned players found</div>'; if(banCount)banCount.textContent='0 shown';return}banList.innerHTML=rows.map(ban=>`<article class="ban-row"><img class="ban-avatar" src="${escapeHtml(ban.skin)}" alt="${escapeHtml(ban.username)}"><div><div class="ban-player-line"><span class="ban-name">${escapeHtml(ban.username)}</span><button class="info-btn" type="button" data-info="${escapeHtml(ban.id)}"><img src="assets/info.svg" alt=""></button></div><span class="ban-date">${escapeHtml(ban.date)}</span></div><div class="ban-reason">${escapeHtml(ban.reason)}<div class="ban-meta">${escapeHtml(ban.type)} • ${escapeHtml(ban.duration)}</div></div><div class="ban-status"><span class="badge ${escapeHtml(ban.status_type)}">${ban.ipban?'<img src="assets/lock.svg" alt="">':''}${escapeHtml(ban.status)}</span></div><div class="ban-action">${actionButton(ban)}</div></article>`).join('');if(banCount)banCount.textContent=`${rows.length} shown`;document.querySelectorAll('[data-info]').forEach(b=>b.addEventListener('click',()=>openBanInfo(b.dataset.info,rows)))}async function loadBans(){if(!banList)return;const search=banSearch?banSearch.value.trim():'';try{const r=await fetch(`api/bans.php?search=${encodeURIComponent(search)}`,{headers:{Accept:'application/json'}});const p=await r.json();if(!p.success){banList.innerHTML=`<div class="error">${escapeHtml(p.error||'Unable to load bans')}</div>`;if(banCount)banCount.textContent='0 shown';return}window.mineacleBans=p.bans||[];renderBans(window.mineacleBans)}catch(e){banList.innerHTML='<div class="error">Unable to load bans. Check your MySQL config and PHP error log.</div>';if(banCount)banCount.textContent='0 shown'}}function openBanInfo(id,rows=window.mineacleBans||[]){const ban=rows.find(r=>String(r.id)===String(id));if(!ban||!banModal)return;document.getElementById('modalAvatar').src=ban.skin;document.getElementById('modalName').textContent=ban.username;document.getElementById('modalStatus').className=`badge ${ban.status_type}`;document.getElementById('modalStatus').innerHTML=`${ban.ipban?'<img src="assets/lock.svg" alt="">':''}${escapeHtml(ban.status)}`;document.getElementById('modalReason').textContent=ban.reason;document.getElementById('modalType').textContent=ban.type;document.getElementById('modalDuration').textContent=ban.duration;document.getElementById('modalDate').textContent=ban.date;document.getElementById('modalAppeal').textContent=ban.appeal_id;document.getElementById('modalEmail').textContent=ban.support_email;document.getElementById('modalDiscord').textContent=ban.discord;const actions=document.getElementById('modalActions'),note=document.getElementById('modalNote');if(ban.ipban){actions.innerHTML='<button class="btn soft disabled" disabled><img src="assets/lock.svg" alt=""> Permanent IP Ban</button>';note.textContent='This is an IP ban. It is marked permanently banned and has no dispute or paid-unban option.'}else if(ban.can_pay){actions.innerHTML=`<a class="btn red" href="${escapeHtml(ban.unban_url)}">${escapeHtml(ban.price)} Pay to be unbanned</a><a class="btn soft" href="${escapeHtml(ban.discord)}" target="_blank" rel="noopener">Contact Discord</a>`;note.textContent='Use the payment option for eligible bans, or contact support if you believe this punishment is incorrect.'}else{actions.innerHTML=`<a class="btn soft" href="${escapeHtml(ban.discord)}" target="_blank" rel="noopener">Contact Support</a>`;note.textContent='This punishment is not currently eligible for paid unban. Contact support if you need more information.'}banModal.classList.add('show')}function closeModal(){if(banModal)banModal.classList.remove('show')}document.querySelectorAll('.copy-ip').forEach(b=>b.addEventListener('click',async()=>{const v=b.dataset.copy||'mineacle.net';try{await navigator.clipboard.writeText(v);showToast('Server IP copied',v)}catch{showToast('Copy server IP',v)}}));if(banSearch){let t=null;banSearch.addEventListener('input',()=>{clearTimeout(t);t=setTimeout(loadBans,180)})}document.querySelectorAll('[data-close-modal]').forEach(b=>b.addEventListener('click',closeModal));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});if(banModal)banModal.addEventListener('click',e=>{if(e.target===banModal)closeModal()});loadBans();